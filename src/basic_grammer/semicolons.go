package main

func main2() int32 {
	//break   If the last token before a newline is an identifier
	//continue
	//fallthrough
	//return
	//++
	//--

	// 如果换行符出现在可以结束语句的令牌之后，则插入分号”。
	x := 8
	return int32(x)

}
func main() {
	//go func() {	for {	dst <- <-src}}()  A semicolon can also be omitted immediately before a closing brace, so a statement such as
}
